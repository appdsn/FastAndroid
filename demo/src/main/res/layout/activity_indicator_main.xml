<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="10dp"
        android:text="强大的智能tab指数器,可以绑定ViewPager一起使用，也可以单独使用，主要功能：\n1：自定义滑动块样式
        \n滑块是一个自定义View，你可以定制这个滑块的各种风格，比如可以画一张图片，一个颜色块滑块等，可以设置颜色，圆角半径，高度，宽度（固定或者跟随tabView宽变化），位置（包括上，中，下三个位置）等；
        可以设置滑块是否覆盖在tabView上面，还是下面；内置的有LineScrollBar（线型颜色块）, DrawableScrollBar（图片）,BezierScrollBar（曲线颜色块）三种样式。
        \n2：自定义tab样式
        \n每个tab都是一个View，你可以自定义这个View，内置的有TextTabView，ClipTabView（剪切显示文字）" />

    <Button
        android:id="@+id/btn1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="doClick"
        android:text="FixTab" />

    <Button
        android:id="@+id/btn2"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="doClick"
        android:text="ScrollTab" />

    <Button
        android:id="@+id/btn3"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="doClick"
        android:text="CircleTab" />

    <Button
        android:id="@+id/btn4"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="doClick"
        android:text="NO ViewPager" />

</LinearLayout>